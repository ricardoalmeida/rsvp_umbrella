<h1>All Events</h1>
<a href="<%= Routes.event_path(@conn, :create) %>" class="btn btn-primary pull-right">Add Event</a>
<h3>Total Count: <%= Enum.count @events %></h3>
<div class="list-group">
  <%= for e <- @events do %>
    <%= render "summary.html", event: e, conn: @conn %>
  <% end %>
</div>
